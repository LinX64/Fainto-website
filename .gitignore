.DS_Store
*.log
.idea/
.vscode/

# Secrets — never commit or deploy
.env
.env.*
*.key
*.pem
*.p12
*.keystore
*.crt
secrets*
credentials*

# Local scratch (not part of the site)
graphify-out/
# Every throwaway probe/harness page is named __*.html — a single rule so a new one can
# never be committed (or mirrored to production) just because it wasn't listed by name.
__*.html
